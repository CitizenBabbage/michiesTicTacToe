
import React from 'react';
 
const Blogs = ( props ) => {
    const dbs = props.dbs, setDBS = props.setDBS;
    // setDebuggingSequencer(debuggingSequencer+1)
    // console.log("debugging sequencer: ", debuggingSequencer) 
    return (
        <h1>You can write your blogs!</h1>
    );
};
 
export default Blogs;